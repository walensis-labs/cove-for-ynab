name: CI
on:
  push: { branches: [main] }
  pull_request:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with: { node-version: 22, cache: pnpm }
      - run: pnpm install --frozen-lockfile
      # build first: packages/core only exposes typed exports via dist/*.d.ts,
      # so a fresh checkout can't typecheck/test apps/mcp against it otherwise.
      - run: pnpm build && pnpm typecheck && pnpm test
  spec-drift:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Diff vendored OpenAPI spec against upstream
        run: |
          curl -fsSL https://api.ynab.com/papi/open_api_spec.yaml -o /tmp/upstream.yaml
          if ! diff -q packages/core/openapi/ynab-v1.yaml /tmp/upstream.yaml >/dev/null; then
            echo "::warning::YNAB OpenAPI spec has drifted upstream — review and re-vendor (pnpm -F @walensis/cove-core gen:api)"
          fi
