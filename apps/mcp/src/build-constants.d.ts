// Injected by tsup at build time (see tsup.config.ts define). In vitest/tsc contexts
// the constant is undefined and server.ts falls back to a dev marker.
declare const __MCP_VERSION__: string | undefined
