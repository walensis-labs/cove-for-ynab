---
"@walensis/cove-mcp": minor
---

Rename local state to `~/.cove/` (was `~/.mcp-for-ynab/`) and identify the server as `cove-for-ynab`, matching the product name. If you have an existing undo journal or ledger, move the directory: `mv ~/.mcp-for-ynab ~/.cove`.
